#### 编码是什么？
* 字符编码是把“字符”转换成“数字”的过程。
* 早期的编码有ASCII（128个字符）、GB2312（中文简体），后来有GBK、BIG5（繁体）、UTF-8、UTF-16。
* CubeMX生成的Keil工程默认是GB2312编码的，如果用UTF-8编辑器打开，会出现中文乱码

#### UTF-8
##### 为什么要改成UTF-8?
* 更通用：UTF-8是Web和现代操作系统的标准。
* 兼容更好：可以跨平台、跨编辑器正常显示。
* 防止乱码：尤其在VSCode、Git、Linux系统下，GB2312会出错。
