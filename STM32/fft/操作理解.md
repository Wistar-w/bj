## 为什么要减去平均值？
#### ADC的单电源供电（最大的罪魁祸首）
单片机ADC只能测量 0V ~ 3.3V 的正电压。如果不加以1.65v提上，就会丢失负半轴。
所以为什么FFT会在0HZ出现一条很长的线是因为0HZ处就是ADC采样的平均值。
>减去平均值是为了“去掉直流噪声”，让FFT能“看清”交流信号的频率。
#### 那么FFT为什么没有负值？
>FFT（快速傅里叶变换）的结果是复数
FFT 输出是复数：X[k]=实部+j⋅虚部

因为幅度是计算方式为 √(实部² + 虚部²)，始终为非负值。
//扩展相位